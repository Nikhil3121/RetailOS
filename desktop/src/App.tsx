/**
 * Kept as a re-export for future use (auth wrapper, error boundary, theme provider).
 * The router mounts the AppShell directly, so this file is not the render root.
 */
export { AppShell as App } from '@/components/layout/AppShell';
