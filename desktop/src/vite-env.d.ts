/// <reference types="vite/client" />

/**
 * Without this reference, `import logo from './logo.png'` has no type and the
 * build fails typecheck. Vite resolves such an import to the emitted asset URL.
 */
