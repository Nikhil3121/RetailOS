"""Database layer: engine/session lifecycle, declarative base, and ORM models.

Import order matters here: `base` declares the metadata, `models` populates it,
and `session` provides the async session factory / request-scoped dependency.
"""
