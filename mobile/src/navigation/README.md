# navigation/

React Navigation setup: RootNavigator switches between auth stack (unauthenticated) and MainTabs (authenticated). MainTabs holds bottom tabs; each tab has its own stack navigator (BillingStack, ProductsStack, etc.). Route param types in types.ts.
