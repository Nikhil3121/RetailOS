# components/

Reusable UI atoms (Button, Card, Chip, Input, Modal). Every component is written with React Native primitives (View/Text/Pressable) + NativeWind class names. Composition-first: no page-specific logic here.
