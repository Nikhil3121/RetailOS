# stores/

Zustand stores — the same pattern as desktop. Persistence swaps localStorage for AsyncStorage via ../lib/storage.ts. Business state (products, sales, etc.) lives in React Query caches, NOT here.
