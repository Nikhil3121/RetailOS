# Root-level shared defaults. Individual services (backend/, desktop/) have their own .env files.
# This file exists so `docker compose` picks up defaults for the infrastructure containers.

POSTGRES_USER=retailos
POSTGRES_PASSWORD=retailos
POSTGRES_DB=retailos
POSTGRES_PORT=5432

REDIS_PORT=6379
